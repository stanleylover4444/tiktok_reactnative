// UI/style.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  avataUser : {
    height : 75,
    width : 75,
    borderRadius : 75,
  }
});

export default styles;
