// UI/style.js
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
   height : 30
  },
  avataUser : {
    height : 75,
    width : 75,
    borderRadius : 75,
  }
});

export default styles;
