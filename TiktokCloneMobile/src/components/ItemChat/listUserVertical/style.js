
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  avataUser : {
    height : 65,
    width : 65,
    borderRadius : 75,
  }
});

export default styles;
